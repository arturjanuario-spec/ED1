#include <stdio.h>

typedef struct {
    char tipo[50];
    int hp;
} Monstro;

int main() {
    int n;
    scanf("%d", &n);
    for (int i = 0; i < n; i++) {
        Monstro m;
        scanf("%s %d", m.tipo, &m.hp);
        printf("Inimigo: %s, HP: %d\n", m.tipo, m.hp);
    }
    return 0;
}
